def parse_task_id(output):    # /data1/shiny-server/dict_lab/www/result/task_    task_prefix = 'task_'    index = output.find(task_prefix)    output = output[index + task_prefix.__len__():]    index = output.find('/')    return output[:index]# parse_task_id('/data1/shiny-server/dict_lab/www/result/task_007/task_007_1.txt')def in_list(target, ls):    for l in ls:        if target in l:            return True    return Falsedef sub():    product_choices = ['cmk', 'cmk_rnn', 'kika', 'gboard', "swift_key", 'typany', 'go']    print product_choices[:2]def file_n():    target_file_path = '/data1/shiny-server/dict_lab/www/corpus/test/en_test_top1w'    file_name = target_file_path[target_file_path.rfind('/') + 1:]    print(' push %s /sdcard/%s' % (target_file_path, file_name))if __name__ == '__main__':    # in_list('com', ['com.kes', 'com.what'])    # sub()    file_n()